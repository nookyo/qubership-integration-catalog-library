name-template: 'Release v{{version}}'
tag-template: 'v{{version}}'
categories:
  - title: '🚀 New Features'
    labels:
      - 'feature'
  - title: '🐛 Bug Fixes'
    labels:
      - 'bug'
change-template: '- {{change}}'
no-changes-template: 'No changes in this release.'
version-template: '$MAJOR.$MINOR.$PATCH$PRERELEASE'
version-resolver:
  major: { labels: [] }
  minor: { labels: [] }
  patch: { labels: [] }
  default: 'patch'
  custom: { value: '${{ env.VERSION }}' }
template: |
  ## What's Changed

  $CHANGES

  **Contributors**:
  $CONTRIBUTORS
draft: false
