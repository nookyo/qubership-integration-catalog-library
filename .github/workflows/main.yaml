name: Custom Action

on:
  workflow_dispatch:

jobs:
  my-action:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run my Action
        # uses: ./actions/xml-replace
        uses: ./actions/tag-checker
        with:
          name: 'Fucker'
          greeting: 'my boy'